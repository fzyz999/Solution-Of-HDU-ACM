#include <cstdio>
#include <cstdlib>

int main()
{
    int T;

    scanf("%d", &T);
    while (T--) {
	int n,x,y;
	scanf("%d%d%d", &n,&x,&y);
	for (int i=0; i < x; i++) {
	    
	}
    }
    
    return 0;
}
