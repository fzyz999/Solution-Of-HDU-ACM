#include <iostream>
#include <string.h>

using namespace std;

int main()
{
     int n,s;
     char num[20],num2[20];

     while(cin>>n)
     {
	  s=0;
	  sprintf(num,"%d",n);
	  sprintf(num2,"%d",n);
	  reverse(num);

	  if(strc)
     }

     return 0;
}
